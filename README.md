# password-decrypt-service

service allows to decrypt the tibco hashed password (e.g. set as GV in project)
use the decrypt.process directly or import the wsdl to your project to use it as a service.
